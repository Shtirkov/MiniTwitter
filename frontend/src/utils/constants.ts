export const ROUTES = {
  ROOT: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  FEED: '/feed',
  PROFILE: '/profile',
} as const;

export const STORAGE_KEYS = {
  TOKEN: 'mt_token',
  USER: 'mt_user',
} as const;
